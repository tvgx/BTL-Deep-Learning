version: "3.9"
services:
  api:
    build: .
    container_name: real-fake-face-api
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=/app/models/best_model.pth
    volumes:
      - ./models:/app/models:ro
    restart: unless-stopped
